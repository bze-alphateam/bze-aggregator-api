services:
  db:
    env_file:
      - ./mysql/mysql.env
    image: mysql
    ports:
      - "${MYSQL_LOCAL_PORT}:${MYSQL_PORT}"
    volumes:
      - "./.data/db:/var/lib/mysql"
      - "./mysql/init:/docker-entrypoint-initdb.d:ro"
